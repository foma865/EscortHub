export function Logo({ className }: { className?: string }) {
  return (
    <div
      className={`bg-blue-500 text-white flex items-center justify-center rounded-full ${className}`}
    >
      EH
    </div>
  );
}

